# 公開手順

このプロジェクトは、React画面とSocket.IO対戦サーバーを1つのRender Web Serviceから配信できます。

1. このフォルダーをGitHubのリポジトリへpushします。
2. Renderで「New」→「Blueprint」を選び、そのGitHubリポジトリを接続します。
3. リポジトリ直下の `render.yaml` を読み込んでデプロイします。
4. 発行された `https://...onrender.com` を参加者全員で開き、同じ合言葉を入力します。

## 注意

- 無料Web Serviceは無通信時にスリープするため、最初のアクセスに時間がかかる場合があります。
- 部屋と対戦状態はサーバーのメモリに保存されます。再デプロイやサーバー再起動で進行中の部屋は消えます。
- 複数インスタンスへの水平スケールには未対応です。現状は1インスタンスで運用してください。
