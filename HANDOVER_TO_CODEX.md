# ゴッドフィールド オリカWebアプリ 引継ぎ資料 (Handover Document for Codex)

## プロジェクト概要
本家「ゴッドフィールド (GodField)」の基本ルールをベースにしつつ、ユーザーが独自のオリジナルカード（オリカ）を作成して遊べる対戦型Webアプリケーションです。

## 技術スタック
*   **フロントエンド:** React (Vite), Vanilla CSS (Tailwind不使用。`index.css` に集約)
*   **バックエンド:** Node.js, Express, Socket.IO
*   **データベース:** なし（データはすべてフロントエンドの `localStorage` に保存し、対戦開始時にSocket経由でサーバーのインメモリー上にロードされる完全サーバーレスライクなアーキテクチャ）

## ディレクトリ構成
*   `frontend/` : Reactアプリケーション
    *   `src/pages/Home.jsx`: ルーム入室画面
    *   `src/pages/Editor.jsx`: オリカ（カスタムカード）作成画面
    *   `src/pages/BaseEditor.jsx`: 本家の基礎カードの設定（画像・名前・説明文の差し替え）を行うマスター画面
    *   `src/pages/GameRoom.jsx`: 対戦画面（本家風クラシックUIレイアウト）
    *   `src/data/baseCards.js`: 本家の基礎カードのステータスマスタデータ
*   `backend/` : Node.js サーバー
    *   `server.js`: 対戦ルームの管理、ゲームコアロジック、ダメージ計算、Socket.io通信

## 現在の進捗状況（完了済みのフェーズ1）
*   **オリカ作成機能:** 画像（PCからの直接アップロード時はBase64圧縮保存）、ステータス、属性などを設定可能。
*   **共通山札システム:** 対戦開始時に「Base Cards（基礎カード）」と「両プレイヤーのオリカ」がサーバー上で合算・シャッフルされ、1つの「共通山札（deck）」が生成される仕様に変更済み（事前のデッキ持ち込み制は廃止）。
*   **ドローシステム:** ゲーム開始時に9枚の手札が配られ、カードを使用するたびに山札から自動補充される（手札上限18枚）。
*   **祈る:** 手札に攻撃可能な武器が1枚もない場合のみ、「祈る」ボタンで1枚ドローしてターンエンドが可能。
*   **対戦UI:** 本家ゴッドフィールドに準拠したクラシックなライトテーマへUI改修済み（正方形の手札、右側にステータス一覧、中央にフィールドとダメージポップアップ）。
*   **属性と状態異常:** 本家の属性相性（火は水で防げる等）と状態異常（風邪、熱病、地獄病、天国病、霧、閃光、幻覚、暗雲）はサーバー側の `server.js` およびフロントエンドの描画ロジックで実装済み。

## 今後の実装予定（未完了のフェーズ2・3）
Codex（次担当）へ引き継ぐ、残りの実装タスクです。

### 【フェーズ2】合体システムと奇跡の永久化
1.  **UIの複数選択対応（合体システム）:**
    *   現在の `GameRoom.jsx` では手札を1枚クリックすると即座にプレイされる仕様。
    *   これを「複数枚選択」できるようにし、「武器」＋「奇跡」などを組み合わせてプレイ（合体）できるようにUIとサーバー処理（`playCard`）を改修する。
2.  **奇跡（魔法）の永久習得:**
    *   現在、奇跡カードは使うと消費（手札から消滅）される。
    *   本家の仕様通り、奇跡カードは一度引く（習得する）と手札に残り続け、MPを支払うことで何度でも使用できるようにする。

### 【フェーズ3】売買・両替システムの完全実装
1.  **売る・買うシステム:**
    *   自分のターンのメインフェーズで、自分の手札を相手に売りつけたり（自分は¥を獲得）、¥を支払って相手の手札からランダムに買い取ったりするシステムを実装する。
2.  **両替（隠れ神殿）のフェーズ実装:**
    *   「両替」アイテムを使用した際、HP・MP・¥を等価交換（レート1:1）できる専用のUI（隠れ神殿フェーズ）を開くようにする。
    *   隠れ神殿で「神器」を購入できるシステムも追加する。

## 特記事項 / 注意点
*   **カードの「お金 (Cost ¥)」について:** 当初はプレイコストとして実装していましたが、本家のルールに合わせて「プレイ時にはお金は消費されない（売値・買値の基準値としてのみ存在する）」仕様に修正済みです。
*   **ステート管理の注意:** バックエンド（`server.js`）はインメモリで状態を管理しているため、`server.js` を書き換えた際は必ず Node プロセスを再起動する必要があります。
*   **画像の保存:** オリカエディタでアップロードされた画像は、Canvasでリサイズ・圧縮された上でBase64文字列として `localStorage` に保存されます。大容量になりすぎないよう制限をかけていますが、データ肥大化には注意してください。

---
以上が現在のプロジェクトの全体像となります。
引き続きフェーズ2およびフェーズ3の実装をよろしくお願いいたします。
